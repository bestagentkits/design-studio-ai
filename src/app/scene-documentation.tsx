export function SceneDocumentation(){return <>
  <h2>Build an editable 3D character</h2><p>Open a 3D project and choose Character authoring. Human controls and agent tools use the same validated geometry, skeleton and animation data.</p>
  <ol><li><strong>Shape.</strong> Remesh unparented sphere primitives or closed document meshes into a continuous surface. Preview first; applying keeps source objects hidden. Use joint-ring lofts for controlled limb topology. Imported asset nodes need editable document geometry first.</li><li><strong>Rig.</strong> Create a quadruped skeleton, inspect its joints and adjust landmarks or bone positions for the model. Auto-bind weights, inspect the heatmap and smooth or mirror weights. Attach eyes and accessories to a named bone.</li><li><strong>Animate.</strong> Pose with FK or bounded IK, then add idle, wag or walk clips. Inspect contact and deformation across the timeline. Morph targets add editable expressions.</li><li><strong>Finish.</strong> Cut and pack UV islands, paint a 512px base-color texture and inspect topology/weights. Export GLB/glTF and reopen the file to verify meshes, skeletons, morph targets, textures and clips.</li></ol>
  <h2>Agent workflow</h2><p>Discover <code>sceneCommands</code> through <code>studio_capabilities</code> or <code>/api/schema</code>. WebMCP <code>studio_scene_command</code> accepts a page ID and command, previews by default, and applies with <code>preview:false</code>. <code>studio_inspect_scene</code> returns compact diagnostics at a requested time without transferring vertex buffers.</p>
  <p>Saved-state equivalents are REST GET/POST <code>/api/projects/:id/scene</code>, MCP <code>inspect_scene</code>/<code>author_scene</code>, and CLI <code>dsa scene inspect</code>/<code>dsa scene command</code>. Writes require the observed revision. CLI commands are available in the current source build; an older release archive may not include them.</p>
  <h2>What the tools check</h2><p>Reports flag degenerate triangles, boundary or non-manifold edges, inconsistent winding, unnormalized skin weights and severe edge stretching in sampled poses. They do not certify self-intersection freedom or animation quality. UV seams can intentionally split vertices.</p><p>Remeshing uses bounded surface sampling rather than semantic quad retopology. Quadruped landmarks and nearest-segment weights are editable starting points. IK targets use mesh-local coordinates and apply a pose; they are not persistent collision constraints. Explicit UV islands use planar projection and need a distortion/overlap check. Paint replaces the base-color map. Topology must be finished before binding or authoring morph targets.</p>
</>;}
