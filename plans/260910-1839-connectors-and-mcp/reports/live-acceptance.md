# Live acceptance — 2026-09-11

## Passed

Public server: `https://docs.mcp.cloudflare.com/mcp`, anonymous. Current application Node DNS-pinned transport negotiated `2026-07-28`; server identified itself as `docs-ai-search` 0.4.13. Discovery returned `search_cloudflare_documentation` and `migrate_pages_to_workers_guide`. A documentation query about public Workers fetch returned real textual results.

The same public server passed the application's project-binding, exact-action approval, refresh recovery and single-dispatch flow on Chromium desktop/mobile, Firefox desktop and WebKit mobile. This used isolated local Studio persistence; it is not evidence of beta connector execution. No private content, provider credentials or paid model request was sent. Test data was disconnected/deleted by the test finally block.

## Required but not performed

- Real MCP write and outgoing OAuth reconnect/revoke against an external authorization server.
- One authorized supported model tool loop with refresh/approval and final unsaved proposal.
- GitHub App selected installation/repository source-to-reviewed-React-PR, inspecting the actual remote commit and PR.
- Google OAuth/Picker selected files and multiple-account isolation; actual PDF/PPTX uploads and native Slides inspection in a designated folder.

GitHub setup advanced on 2026-09-11: the user registered Design Studio AI Beta under bestagentkits (App ID 4907218), generated credentials, and installed it as installation 160820782. Browser inspection confirms only the private repository bestagentkits/design-studio-ai-beta-acceptance (repository ID 1365618484) is selected. App-authenticated GitHub API verification confirms the installation is active, with contents/pull_requests write and metadata read permissions. The private key successfully authenticated the expected App identity.

All five GITHUB_CONNECTOR_* configuration values were uploaded to the isolated beta Worker through non-echoing stdin; secret-name inspection confirms their presence alongside the preserved ENCRYPTION_KEY. No credential values are recorded here. The real installation.created webhook returned HTTP 200; the earlier ping returned 503 before configuration. Beta health returned HTTP 200 with ok=true, while connectorsEnabled remains false. This verifies App setup and a real signed webhook, not the user OAuth/source/export workflow. Studio beta browser login subsequently succeeded. CONNECTORS_ENABLED was then set to true on the isolated beta Worker for authorized live acceptance; /api/config confirmed it. Production configuration was not changed. The browser created a pending GitHub connection and the deployed authorization route generated the expected GitHub OAuth consent page for installation 160820782, with the beta callback and PKCE S256. Human OAuth consent completed successfully; Studio reports the expected connected GitHub user/installation. In project 668e9dc7-0070-4d8a-88f2-2d0a2472b300, the deployed connector listed the single selected repository, resolved main to commit 2548532c23bc3b49628ddd0a2f54b493d326437b (also checked independently through GitHub), and imported README.md as a 104-byte text/markdown snapshot (b6d5192e-985f-4864-a318-eecc103c52b9). An export destination limited to studio-export was saved; React export preparation produced a review of 14 added files and no deletions, operation 56bc62a5-3536-4d5a-ae7b-8645d437cca5. The action remains awaiting approval; no successful remote PR is claimed yet.

Google registration, model and external OAuth/write MCP acceptance remain pending. Do not mark overall acceptance complete or enable general connector execution based on contract fixtures, public documentation search, green CI or a disabled beta deployment.

Cloudflare remote preview also passed the fixed public documentation search through `createWorkersConnectorFetch` and the same `withMcpClient`: profile 2026-07-28, two discovered tools, 2,038 text bytes, isError false. Executable probe: `probes/public-mcp-edge.mjs`. Preview used nodejs_compat/global_fetch_strictly_public, no application database/bucket/credentials and no caller-supplied targets. The remote preview process was stopped after the request. This does not enable beta routes or establish native-provider/model acceptance.

## Connector modal polish

User requested a more polished modal UI during the GitHub acceptance review. Connector dialogs now use consistent padding, form sizing, focus states and mobile layouts. GitHub export review presents repository, base branch, directory, pinned commit and file changes before expandable exact payload details. Approval and execution behavior are unchanged. Typecheck, production build, all 367 tests and all 8 connector browser tests passed (Chromium desktop/mobile, Firefox and WebKit). Browser tests include a real public MCP action, refresh recovery and approval. Commit e336ea7 passed beta CI run 34579382720 and deployed. Live browser inspection then identified a clipped sticky action bar at a short viewport; CSS-only follow-up 3f24755 fixes its bottom offset. Its production build passed and was deployed directly to isolated beta as Worker version 2e562c84-8bcf-4914-b7d9-3a90ad440e52. A second live screenshot confirms all action buttons are fully visible with the actual 14-file GitHub review. CI run 34579911691 subsequently completed successfully for that exact SHA, including deploy. Cloudflare confirms active version 159c9aed-7c55-440e-a31e-d1b7ebdee4a7. See [full plan verification](verification-260911-1539-plan-completion.md). No approval or external write was performed during UI verification.
