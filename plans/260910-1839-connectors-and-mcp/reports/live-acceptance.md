# Live acceptance — 2026-09-11

## Native Slides and Docs follow-up — 2026-09-11

Native export is now verified for a supported three-page slide design: project `56532c84-060d-44f2-918d-e82688eda4f8`, operation `6395f55b-55bc-41d5-887e-112b518fe9c2`, presentation `1hDUuRo2h7zOvX8pU5OMSOMZQz6nnO6q0U0D-80Vt0jo`. Actual Google Slides editor shows exactly three populated slides with the expected template headings, text and shapes. This followed two additional live findings: Google adds a default slide on creation, and the converter previously ignored layout and silently represented web components as empty shapes. Creation now requests only `presentationId,slides(objectId)` and removes those just-created default slides in the population batch. Conversion resolves the shared layout on a clone and rejects unsupported web components before any remote creation. Legacy and connection-backed export share these behaviors. Existing web acceptance files created before this repair are diagnostic artifacts, not export-fidelity evidence.

Google Docs source `11P8TeO0_5Q6ILlhUkiqYcT5sIafNlRU3XFd0vAdDsNU` was explicitly selected through Picker and imported as snapshot `6e0ef600-08c5-47ae-b47b-8ed2dee6dccc`. Its actual stored 178 bytes match SHA-256 `5984aa24c4f8362201f54940e998fce84bc3b4ed6c7b143499675f62882c1324`; the authored Vietnamese sentence survives UTF-8 extraction. A new empty document was initially opened by Google's default personal account before the intended work account was explicitly selected; that empty document is `1qRL1aABboAIje01vJdVfnrbK4Vg1Hv64hszMsURFaII`. It is not source evidence and no content was entered there.

All 369 unit/integration tests pass after the layout/component repair; typecheck and application/docs build pass. Additional focused tests cover layout coordinates without mutating the saved design, oversized creation metadata, default-slide deletion, and rejection of unsupported components. Original uncertain operations remain unchanged; no blind replay or manual success marking was used. Remaining live gates still include provider/model and external OAuth/write MCP credentials, second Google account isolation, disconnect/revoke, image/text source coverage and independent remote-download byte comparison.

## Slides response diagnosis — 2026-09-11 10:15 UTC

A separately named diagnostic presentation reproduced the failure with sanitized server diagnostics: `native_connector_response_rejected`, origin slides.googleapis.com, reason `provider_response_too_large`; the operation then reported `invalid_provider_response` without a recorded ID. The initial execute telemetry took 2,955 ms wall / 57 ms CPU, so the earlier timeout hypothesis was not supported.

Cause: nativeJson limits JSON to 262,144 bytes, while unfiltered Slides creation returns full presentation/layout metadata. Both connected and legacy creation calls now request `fields=presentationId`, retaining bounded response handling. [Google partial-response documentation](https://developers.google.com/workspace/slides/api/guides/performance) supports this parameter. The provider test now supplies oversized layout metadata when no projection is requested: before the fix both identity-retention scenarios failed; after the fix success and partial-population identity tests pass. All 367 tests, typecheck and build pass. Fixed beta code deployed as `3fb6c4c4-570b-4aab-a850-1099eec43a57`; final live acceptance is recorded separately below when observed. Diagnostics emit fixed error classifications only, never credentials, request bodies or provider response contents.

## Current Google acceptance checkpoint — 2026-09-11 10:05 UTC

This checkpoint supersedes earlier pending/setup statements below. Google OAuth and real Picker are working on isolated beta. A native-dialog top-layer bug initially placed Picker behind the project modal. Commit `e020dee3390ab7523508ce69223983305126e298` temporarily closes the mounted parent dialog while Picker is active and restores it on completion/cancel. Typecheck, all 367 tests and production build passed; [CI 34585612298](https://github.com/bestagentkits/design-studio-ai/actions/runs/34585612298) independently completed verify and deploy successfully. Active beta version was verified as `d22343f8-bc7e-4847-a210-a0845a45940d`. Live in-app browser checks verified folder selection, source selection, cancellation and parent modal restoration; this Picker-specific check does not establish other browsers.

Created designated folder `Design Studio AI Beta Acceptance 2026-09-11`, ID `1EqAS2GoxSvakp17RSf_McqHnV-9_KXzk`. Its general access was changed to Restricted. Selected it through real Picker and saved the destination binding. No claim is made about file-level sharing inherited from organization defaults.

- PDF operation `7f4340d0-2fff-4cbe-9f9c-186ac9827f9b` succeeded after exact-action approval. Remote file `1qKgWjy0PrbjDbTWxYk5yd8xivpSLrlYX`, filename `studio-beta-acceptance.pdf`. Prepared artifact: 42,320 bytes, SHA-256 `13f7f373338dc092fa94112b8d1ccef3e4e1513280c12c4d01a47ee63f4c7eab`. Downloaded the reviewed R2 artifact and verified one PDF page and expected design text. Opened the actual remote Drive PDF: one page with the expected content.
- Selected that actual PDF again through source Picker. Snapshot `faa34d8f-ebc8-4178-9cd9-6223e7714829` is available, text/plain, 280 bytes, provider version 5, SHA-256 `04c7e47bbbbed6f11a892a5cbe21edcb4ebf75601f01378da26f61c3abe0a6aa`. This establishes real Drive PDF fetch/extraction, not Docs/image acceptance.
- PPTX operation `68d0f133-61f8-4422-9ef6-a811120720a4` succeeded after review/approval. Remote file `166AiXM5WR6JJImiZrk8Krx6htNsTTwpG`, filename `studio-beta-acceptance.pptx`. Prepared artifact: 138,887 bytes, SHA-256 `145454daa5747a7d7aa183b7648479c41cf1d99c02792f2393f7f46bf7b14771`. Reviewed R2 bytes passed ZIP integrity and XML parsing; one slide and two embedded media entries. Actual Drive preview opened as one page and visually matched the design. Browser download buttons did not produce local files: independent remote-byte hash comparison remains unverified for both formats.
- Native Slides operation `573d5b94-11b4-4485-b55d-d952bfe7b85c` returned `outcome_unknown` with no recorded remote ID. Independent Google Slides UI inspection found the newly created `Studio Beta Native Slides Acceptance`, presentation ID `1eCJu-bVRBGSxRTfXIEMiTKFfktGAD4vZnYDOX7JYHEc`. It is empty and content population did not finish. Google Slides API is enabled in the correct Cloud project. No repeated create, manual database repair, deletion or success marking was performed. The creation-response failure cause is not established; safe recovery and native Slides acceptance remain open. Keep this exact operation/presentation pair for investigation.

The four Google configuration fields are saved in the user-requested local Obsidian credential note, without values in repository or tool output. Production remains untouched. Remaining acceptance includes Docs/text/image sources, independent downloaded bytes, native Slides recovery, second-account isolation, disconnect/revoke, real model tool loop and external OAuth/write MCP. No full-plan completion is claimed.

## Google registration checkpoint — 2026-09-11 09:30 UTC

Created `design-studio-ai-beta` (project number `75025282031`) in `wearetopgroup.com`, using the user's explicitly selected Company Billing Account. Google Drive, Slides and Picker APIs are enabled. Created the `Design Studio AI Beta Connector` web OAuth client with origin `https://beta.studio.agentkit.best` and exact callback `https://beta.studio.agentkit.best/api/connectors/google-drive/callback`. Consent is External / Testing; the currently signed-in owner account is listed as a test user. Configured only openid, email and drive.file scopes.

Created `Design Studio AI Beta Picker` API key restricted to Google Picker API and website `https://beta.studio.agentkit.best/*`. Credential values are excluded from this report. The OAuth download button did not produce a matching JSON file in Downloads, and the browser tabs were subsequently closed. Both credential values were still available in the active browser-tool session and were transferred through an isolated loopback form to a mode-0600 temporary file without printing their values. The loopback server was stopped after transfer. Wrangler confirmed all four GOOGLE_CONNECTOR / GOOGLE_PICKER configuration secrets were uploaded successfully to the isolated beta Worker. The deployed Google OAuth flow then completed with the configured test account and drive.file consent; Studio reports the Google Drive connection as connected. Actual Picker/source/export acceptance remains pending. No production configuration changed.

## Passed

Public server: `https://docs.mcp.cloudflare.com/mcp`, anonymous. Current application Node DNS-pinned transport negotiated `2026-07-28`; server identified itself as `docs-ai-search` 0.4.13. Discovery returned `search_cloudflare_documentation` and `migrate_pages_to_workers_guide`. A documentation query about public Workers fetch returned real textual results.

The same public server passed the application's project-binding, exact-action approval, refresh recovery and single-dispatch flow on Chromium desktop/mobile, Firefox desktop and WebKit mobile. This used isolated local Studio persistence; it is not evidence of beta connector execution. No private content, provider credentials or paid model request was sent. Test data was disconnected/deleted by the test finally block.

## Required but not performed

- Real MCP write and outgoing OAuth reconnect/revoke against an external authorization server.
- One authorized supported model tool loop with refresh/approval and final unsaved proposal.
- GitHub App selected installation/repository source-to-reviewed-React-PR, inspecting the actual remote commit and PR.
- Google OAuth/Picker selected files and multiple-account isolation; actual PDF/PPTX uploads and native Slides inspection in a designated folder.

GitHub setup advanced on 2026-09-11: the user registered Design Studio AI Beta under bestagentkits (App ID 4907218), generated credentials, and installed it as installation 160820782. Browser inspection confirms only the private repository bestagentkits/design-studio-ai-beta-acceptance (repository ID 1365618484) is selected. App-authenticated GitHub API verification confirms the installation is active, with contents/pull_requests write and metadata read permissions. The private key successfully authenticated the expected App identity.

All five GITHUB_CONNECTOR_* configuration values were uploaded to the isolated beta Worker through non-echoing stdin; secret-name inspection confirms their presence alongside the preserved ENCRYPTION_KEY. No credential values are recorded here. The real installation.created webhook returned HTTP 200; the earlier ping returned 503 before configuration. Beta health returned HTTP 200 with ok=true, while connectorsEnabled remains false. This verifies App setup and a real signed webhook, not the user OAuth/source/export workflow. Studio beta browser login subsequently succeeded. CONNECTORS_ENABLED was then set to true on the isolated beta Worker for authorized live acceptance; /api/config confirmed it. Production configuration was not changed. The browser created a pending GitHub connection and the deployed authorization route generated the expected GitHub OAuth consent page for installation 160820782, with the beta callback and PKCE S256. Human OAuth consent completed successfully; Studio reports the expected connected GitHub user/installation. In project 668e9dc7-0070-4d8a-88f2-2d0a2472b300, the deployed connector listed the single selected repository, resolved main to commit 2548532c23bc3b49628ddd0a2f54b493d326437b (also checked independently through GitHub), and imported README.md as a 104-byte text/markdown snapshot (b6d5192e-985f-4864-a318-eecc103c52b9). An export destination limited to studio-export was saved; React export preparation produced a review of 14 added files and no deletions, operation 56bc62a5-3536-4d5a-ae7b-8645d437cca5. The action remains awaiting approval; no successful remote PR is claimed yet.

Google registration, model and external OAuth/write MCP acceptance remain pending. Do not mark overall acceptance complete or enable general connector execution based on contract fixtures, public documentation search, green CI or a disabled beta deployment.

Cloudflare remote preview also passed the fixed public documentation search through `createWorkersConnectorFetch` and the same `withMcpClient`: profile 2026-07-28, two discovered tools, 2,038 text bytes, isError false. Executable probe: `probes/public-mcp-edge.mjs`. Preview used nodejs_compat/global_fetch_strictly_public, no application database/bucket/credentials and no caller-supplied targets. The remote preview process was stopped after the request. This does not enable beta routes or establish native-provider/model acceptance.

## Connector modal polish

User requested a more polished modal UI during the GitHub acceptance review. Connector dialogs now use consistent padding, form sizing, focus states and mobile layouts. GitHub export review presents repository, base branch, directory, pinned commit and file changes before expandable exact payload details. Approval and execution behavior are unchanged. Typecheck, production build, all 367 tests and all 8 connector browser tests passed (Chromium desktop/mobile, Firefox and WebKit). Browser tests include a real public MCP action, refresh recovery and approval. Commit e336ea7 passed beta CI run 34579382720 and deployed. Live browser inspection then identified a clipped sticky action bar at a short viewport; CSS-only follow-up 3f24755 fixes its bottom offset. Its production build passed and was deployed directly to isolated beta as Worker version 2e562c84-8bcf-4914-b7d9-3a90ad440e52. A second live screenshot confirms all action buttons are fully visible with the actual 14-file GitHub review. CI run 34579911691 subsequently completed successfully for that exact SHA, including deploy. Cloudflare confirms active version 159c9aed-7c55-440e-a31e-d1b7ebdee4a7. See [full plan verification](verification-260911-1539-plan-completion.md). No approval or external write was performed during UI verification.

## GitHub export acceptance — 2026-09-11 08:49 UTC

The prepared artifact was downloaded read-only from isolated beta R2 after the browser download did not produce a local file. Its 23,087 bytes match SHA-256 `47a243d2d0da8df4fdb302c48a0bc91d29dc24567ed9ca1fc61be42a84420dbd`. The ZIP contains 14 real React project files; paths were checked for traversal and workflow changes. Installing its declared packages and building the extracted project succeeded. This build emitted the normal large-chunk warning; it is not proof of all design interactions.

The existing operation `56bc62a5-3536-4d5a-ae7b-8645d437cca5` was approved once and executed through the deployed Studio UI, which reported `succeeded`. It produced [acceptance PR #1](https://github.com/bestagentkits/design-studio-ai-beta-acceptance/pull/1), branch `studio/export-56bc62a5-3536-4d5a-ae7b-8645d437cca5`, commit `74f39ecf52569a06480a7199e8e6c053b1085d1b` and tree `4966059f086aa9ba772eb03fb04a0304839f1dfe`.

Independent GitHub API inspection verified every one of the 14 remote blob hashes against the reviewed ZIP. The pull request is open and unmerged; main remains `2548532c23bc3b49628ddd0a2f54b493d326437b`. Exactly one PR matches the operation branch. No forced push, merge or duplicate preparation was performed. The existing GitHub export contract test separately verifies reconciliation after a lost PR response without another write; this live acceptance does not inject a provider failure.

Cloudflare's authenticated telemetry query for the beta Worker returned the actual execute event: outcome `ok`, CPU 196 ms, wall time 17,627 ms. Its preceding decision event used 13 ms CPU and 298 ms wall time. These are single observed invocations, not percentile or worst-case limits. The API redacts project/operation IDs in request paths; association uses the isolated beta service, path and execution time window. Raw telemetry stays in a private temporary local file. Query fields follow [Cloudflare telemetry API](https://developers.cloudflare.com/api/resources/workers/subresources/observability/subresources/telemetry/methods/query/).

Google Cloud was opened for the authorized setup and requires interactive account re-verification. The user was handed that login step. Beta still has no configured model provider; a provider/model choice or credential location was requested without requesting raw secrets in chat. External OAuth/write MCP and PDF/model runtime measurements remain pending.

Remote PDF extraction and CPU boundaries were subsequently exercised in an isolated authenticated Worker; see [PDF edge measurements](pdf-edge-budget-results.md). Peak memory and representative model/tool-loop budgets remain unverified.
