# Export runtime delivery

Status: DONE_WITH_CONCERNS

## Implemented

- Actual React DocumentView/layout/components render before PNG/PDF capture. Structured PowerPoint pages use actual DOM rasterization. Legacy PowerPoint text/primitives remain editable and legacy 3D raster behavior remains intact.
- Shared configured 3D scene/camera used for captured pages, public viewer and cloud animation rendering. Animation updates existing scene transforms rather than freezing initial geometry.
- Browser exports include real GLB/glTF (mesh, skin, materials, baked timeline tracks), plus runnable React source ZIP for Web/App interface documents.
- React manifest generated by build-renderer from repository-owned source files. Archive includes document JSON, actual React component/layout runtime, dependencies, Vite configuration and portable assets. No document text becomes executable source.
- HTML browser export embeds trusted viewer and portable media.
- Public slide responses strip speaker notes, including previously saved snapshots, without modifying owner documents.
- Scene exporter samples legacy 2D coordinates correctly, limits animation sample allocation, disposes skeleton textures and releases completed/partially built resources when scene construction fails.

## Verification

- `node scripts/build-renderer.mjs` passed.
- `npx tsc --noEmit --pretty false` passed.
- `npx tsx --test tests/structured-export.test.ts tests/export.test.ts tests/published-notes.test.ts`: 10 passed.
- Actual React ZIP extracted into an isolated temporary directory; Vite production build succeeded using installed repository dependencies.
- GLB/glTF loaded with actual GLTFLoader: geometry, red material, metalness 0.3, roughness 0.7, one skeleton joint and animation clip preserved. At t=0.5, loaded skinned vertex matches source and moves to y=1.5 from bind y=0.
- PowerPoint embedded PNG opened and inspected. Real styled controls/text rendered. Found template hero wrapping defect, reported to parent to fix in owning template rather than changing export layout.
- All started browsers closed; temporary source archive extraction removed.

## REST/MCP/CLI follow-up blocked by privacy hook

Server new-format additions were fully reverted: `server/exports.ts` has no worker diff. Browser React/GLB/glTF exports work; network export contracts retain prior formats. Renderer already exposes a trusted scene encoder for future route integration.

A normal patch to existing server asset access was rejected because the privacy hook parsed the TypeScript context environment database property as a filename. No bypass attempted. Parent owns user approval and cross-client completion.

After approval, implement these concrete changes in server/exports.ts:

1. Add react/glb/gltf to options enum and MIME map; React filename extension is zip. Limit React to web/wireframe, require a model3d node for scene formats.
2. Factor the existing owned asset lookup/read into a cached embedding helper, retain owner validation and 30 MB total unique bytes limit, and rewrite both node source URLs and asset registry URLs. This is needed for scene textureAssetId references as well as React archive assets.
3. React route fetches `/studio-react-runtime.json` from the existing static-assets binding, rejects missing manifest with renderer_not_built, calls shared `createReactArchive(document, manifest)`, and returns archive bytes with the existing private/no-store headers.
4. GLB/glTF stay inside the isolated browser and external-request interception. Extend binary dispatch to call `studioRenderer.scene(document, pageIndex, format)` and decode its base64 result, preserving existing authorization, revision, render-budget and error handling.
5. Exclude React from browser-only raster budgets. Continue import-required/external-network protection for cloud scene rendering. Reject non-owned remote texture references before rendering; asset byte bounds still apply.
6. Extend MCP format enum/text handling and CLI allowlist/help to the same supported formats only after the REST dispatch works. Add real authenticated route tests for all three and tenant/revision rejection tests.

## Limits

- Structured PowerPoint pages are images; native editing retained only for legacy text/primitive layouts.
- Browser video fallback is silent; cloud path retains existing media mixing.
- Remote HTTPS assets require successful loading; imported glTF external dependencies are not made portable automatically. GLB is the supported imported model asset container.
- REST/MCP/CLI new formats pending the blocked server integration above.

Unresolved question: user approval for the privacy-hook-rejected server patch.
