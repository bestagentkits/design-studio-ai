# Motion playback and save/share recovery

Status: verified locally; PR and production delivery pending.

Outcome: preserve character frames across Live document refreshes; saved content stays clean; Save cannot indefinitely disable Share; Preview remains visible in both modes.
Constraints: preserve local edits, optimistic revisions, asset ownership and explicit snapshot publication. Never automatically retry an uncertain write.
Non-goals: changing animation artwork, public API schemas or provider behavior.

- [x] Diagnose cache invalidation, JSON ordering/default normalization, unbounded synchronization requests, and mode-toggle discoverability.
- [x] Cache image sources by ID/URL; atomically swap decoded replacements and retain prior frame.
- [x] Normalize document fingerprints consistently; reconcile auxiliary save responses.
- [x] Bound document request and lock waits; preserve uncertain outcomes and sync-lock ownership.
- [x] Separate Edit/Preview controls.
- [x] Verify unit/full tests and real-browser save, share, live-refresh, timeout and mobile workflows.
- [ ] Review and finish release delivery.

Acceptance: cloned unchanged assets cause no decoding/readiness drops; normalized saves leave Save disabled and Share enabled; real user content/order changes remain dirty; stalled sync returns controls without overwriting pending edits; original scene/character/export tests pass.

Verification: 311 unit/integration tests; typecheck and production build passed. Motion regressions passed Chromium desktop/mobile, Firefox and WebKit. Desktop Live/Save + Undo regressions passed. Independent review approved after bounding auxiliary save paths. Public docs regenerated by the build. Final mobile rerun and CI/deployment tracked separately.
